<header class="site-header">
    <div class="container nav-container">
        <a href="{{ '/' | url }}" class="site-logo" style="display: flex; align-items: center; gap: 0.75rem; color: var(--color-slate-900); text-decoration: none;">
            <!-- Logo for light background -->
            <img src="{{ '/assets/logo-dark.png' | url }}" alt="{{ site.name }} Logo" style="height: 60px;">
            <span style="font-weight: 800; font-size: 1.5rem; letter-spacing: -0.02em;">TriGrove Consulting</span>
        </a>
        <nav class="main-nav">
            <ul>
                <li><a href="{{ '/' | url }}" class="nav-link {{ 'active' if page.url == '/' }}">Home</a></li>
                <li><a href="{{ '/services/' | url }}" class="nav-link {{ 'active' if '/services/' in page.url }}">Services</a></li>
                <li><a href="{{ '/advisors/' | url }}" class="nav-link {{ 'active' if '/advisors/' in page.url }}">Advisors</a></li>
                <li><a href="{{ '/approach/' | url }}" class="nav-link {{ 'active' if '/approach/' in page.url }}">Approach</a></li>
                <li><a href="{{ '/contact/' | url }}" class="btn btn-primary">Request a consult</a></li>
            </ul>
        </nav>
    </div>
</header>
